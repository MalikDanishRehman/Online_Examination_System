// function allowRoles(...roles) {
//     return (req, res, next) => {
//         const { role } = req.body; // simple version (JWT later)
//         if (!roles.includes(role)) {
//             return res.status(403).send("Access denied");
//         }
//         next();
//     };
// }

// module.exports = { allowRoles };


//not doing this rn, code made to accomodate it tho.
// to do, JWT